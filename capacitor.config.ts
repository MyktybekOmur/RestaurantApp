import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RestaurantTest',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
